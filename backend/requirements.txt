# Step 1 (graph skeleton) needs only langgraph.
langgraph>=0.2

# Step 2 (policy engine):
pyyaml>=6              # policy.yaml rule engine
pytest>=8              # unit tests

# Step 3 (real worker + LLM judge) — either provider works; key picked from .env:
openai>=1.30           # OpenAI-backed worker/judge (OPENAI_API_KEY)
anthropic>=0.40        # Anthropic-backed worker/judge (ANTHROPIC_API_KEY)
python-dotenv>=1       # load API keys from .env

# Step 5 (REST + WebSocket server):
fastapi>=0.110
uvicorn[standard]>=0.29
