# agents/ — placeholder (Step 3)

Will hold the real agent implementations that replace the skeleton stubs:
- `worker.py`   — Claude API call with tool definitions; proposes actions only.
- `guardian.py` — `check_policy_rules()` (Stage 1) + `claude-sonnet-4-6` judge (Stage 2).

Empty until Step 3 — see docs/03-roadmap.md.
