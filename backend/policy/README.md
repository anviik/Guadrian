# policy/ — placeholder (Step 2)

Will hold `policy.yaml`: the hard, deterministic rules the guardian checks before
ever invoking the LLM judge (path must be inside the sandbox, max records per action,
no paid API without confirmation, etc.). Empty until Step 2 — see docs/03-roadmap.md.
