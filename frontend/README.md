# frontend/ — placeholder (Step 6)

React + Vite + TypeScript. Live two-node agent graph (worker ● ─ ● guardian),
action-log cards, human-approval modal on PAUSE, edges colored by verdict
(green=allow, red=block, amber=pause), and a visible rollback revert. Subscribes once
to the backend WebSocket and renders state deltas — no polling. Empty until Step 6.
